<% provide(:header_title, "お知らせ一覧") %>

<% if @reports.present? %>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

      <% @reports.each do |report| %>
        <tr>
          <td><%= report.title %></td>
        </tr>
      <% end %>
    <% else %>
      <div class="">
        お知らせはありません
      </div>
  </tbody>
</table>
<% end %>
